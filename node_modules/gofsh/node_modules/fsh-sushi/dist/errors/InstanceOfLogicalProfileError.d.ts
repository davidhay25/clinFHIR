import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class InstanceOfLogicalProfileError extends Error implements WithSource {
    instanceName: string;
    instanceOf: string;
    sourceInfo: SourceInfo;
    constructor(instanceName: string, instanceOf: string, sourceInfo: SourceInfo);
}
