"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ParentNameConflictError = void 0;
class ParentNameConflictError extends Error {
    constructor(childName, parentName, parentType, sourceInfo) {
        super(`Parent ${parentName} for ${childName} is defined as ${/^[AEIOU]/.test(parentType) ? 'an' : 'a'} ${parentType} in FSH, which can't be used as a parent. A FHIR definition also exists with this name. If you intended to use that, reference it by its URL.`);
        this.childName = childName;
        this.parentName = parentName;
        this.parentType = parentType;
        this.sourceInfo = sourceInfo;
    }
}
exports.ParentNameConflictError = ParentNameConflictError;
//# sourceMappingURL=ParentNameConflictError.js.map