import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class ParentNameConflictError extends Error implements WithSource {
    childName: string;
    parentName: string;
    parentType: string;
    sourceInfo: SourceInfo;
    constructor(childName: string, parentName: string, parentType: string, sourceInfo: SourceInfo);
}
