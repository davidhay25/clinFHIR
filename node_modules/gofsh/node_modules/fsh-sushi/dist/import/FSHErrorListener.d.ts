import { TextLocation } from '../fshtypes';
import { Recognizer, Token } from 'antlr4';
import { ErrorListener } from 'antlr4/error';
export declare class FSHErrorListener extends ErrorListener {
    readonly file: string;
    constructor(file: string);
    syntaxError(recognizer: Recognizer, offendingSymbol: Token, line: number, column: number, msg: string): void;
    buildErrorMessage(recognizer: Recognizer, offendingSymbol: Token, line: number, column: number, msg: string): {
        message: string;
        location: TextLocation;
    };
}
