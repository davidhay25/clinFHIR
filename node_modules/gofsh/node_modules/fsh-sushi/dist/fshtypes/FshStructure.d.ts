import { FshEntity } from './FshEntity';
import { Rule } from './rules';
export declare abstract class FshStructure extends FshEntity {
    name: string;
    id: string;
    parent?: string;
    title?: string;
    description?: string;
    rules: Rule[];
    constructor(name: string);
    get constructorName(): string;
    metadataToFSH(): string;
}
