import { PackageJSON } from '../package/PackageJSON';
import { VirtualPackage, VirtualPackageOptions } from './VirtualPackage';
export declare class InMemoryVirtualPackage implements VirtualPackage {
    private packageJSON;
    private resources;
    private log;
    private allowNonResources;
    private registeredResources;
    constructor(packageJSON: PackageJSON, resources: Map<string, any>, options?: VirtualPackageOptions);
    registerResources(register: (key: string, resource: any, allowNonResources?: boolean) => void): Promise<void>;
    getPackageJSON(): PackageJSON;
    getResourceByKey(key: string): any;
}
