import { Rule } from './Rule';
export declare class ConceptRule extends Rule {
    code: string;
    display?: string;
    definition?: string;
    system: string;
    constructor(code: string, display?: string, definition?: string);
}
