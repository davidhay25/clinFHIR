import { FshEntity } from './FshEntity';
import { SdRule } from './rules';
export declare class Profile extends FshEntity {
    name: string;
    id: string;
    parent?: string;
    title?: string;
    description?: string;
    mixins?: string[];
    rules: SdRule[];
    constructor(name: string);
}
