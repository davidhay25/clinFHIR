"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class ParentDeclaredAsProfileIdError extends Error {
    constructor(name, id, sourceInfo, fhirResourceUrl) {
        super(`Profile "${name}" cannot declare "${id}" as both Parent and Id.${fhirResourceUrl
            ? ` It looks like the parent is an external resource; use its URL (e.g., ${fhirResourceUrl}).`
            : ''}`);
        this.name = name;
        this.id = id;
        this.sourceInfo = sourceInfo;
        this.fhirResourceUrl = fhirResourceUrl;
    }
}
exports.ParentDeclaredAsProfileIdError = ParentDeclaredAsProfileIdError;
//# sourceMappingURL=ParentDeclaredAsProfileIdError.js.map