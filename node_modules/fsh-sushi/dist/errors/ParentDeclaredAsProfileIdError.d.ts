import { WithSource } from './WithSource';
import { SourceInfo } from '../fshtypes';
export declare class ParentDeclaredAsProfileIdError extends Error implements WithSource {
    name: string;
    id: string;
    sourceInfo: SourceInfo;
    fhirResourceUrl: string;
    constructor(name: string, id: string, sourceInfo: SourceInfo, fhirResourceUrl: string);
}
